<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Silently forward all requests to the public subfolder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>